name: Tests

on:
  push:
    branches: [ "main" ]
  pull_request:
    branches: [ "main" ]

jobs:
  pytest:
    runs-on: ubuntu-latest
    timeout-minutes: 20

    strategy:
      matrix:
        python-version: ["3.9"]

    env:
      PYTHONPATH: .
      RIDER_PI_CI: "1"
      RIDER_PI_MOCK_HW: "1"

    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Set up Python ${{ matrix.python-version }}
        uses: actions/setup-python@v5
        with:
          python-version: ${{ matrix.python-version }}
          cache: pip

      - name: Install test deps (no package build)
        run: |
          set -euo pipefail
          python -m pip install --upgrade pip
          # (opcjonalnie) zależności projektu – bez maskowania błędów
          if [ -f requirements.txt ]; then pip install -r requirements.txt; fi
          if [ -f requirements-dev.txt ]; then pip install -r requirements-dev.txt; fi
          # minimalny zestaw dla testów + piny zgodne z RPi
          pip install pytest pytest-cov
          pip install pillow==8.1.2
          pip install "flask<3" "werkzeug<3"
          pip install "requests>=2.31,<3"

      - name: Run tests
        run: |
          pytest -q --maxfail=1 --disable-warnings -k "not slow" \
                 --cov=. --cov-report=term-missing --cov-report=html

      - name: Upload coverage (HTML)
        if: ${{ always() }}
        uses: actions/upload-artifact@v4
        with:
          name: coverage-html
          path: htmlcov/
          if-no-files-found: ignore